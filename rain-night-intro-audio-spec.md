# 《清风渡》BGM 小样制作标准：雨夜引子

- **任务编号**：P0-T4 · 音频方向验证
- **资源 ID**：`bgm_rain_intro`
- **版本**：v0.1（小样验证用）
- **适用场景**：`scene_001_rain_night`，雨夜山道开场；玩家在抵达清风渡客栈前的孤身行路段。
- **交付目标**：制作一份可供作曲、混音与 Godot 接入验证的小样制作标准；本文件不是最终 BGM 成品需求，也不覆盖后续 12 首 BGM 的量产规格。

---

## 1. 叙事与情绪定位

### 1.1 场景功能

雨夜引子服务于“雨夜山道 → 清风渡客栈”的开篇过渡：先让玩家感到山路空寂、方向不明，再以微弱的前行感牵引至客栈灯火。它必须为古玉、观澜阁与客栈伙计暗线预留不安感，但不得提前泄露真相，也不得使用战斗、恐怖突袭或强烈悲情的表达。

### 1.2 情绪曲线

| 时间段 | 情绪重点 | 叙事功能 |
|---|---|---|
| 0:00–0:20 | 空旷、湿冷、孤身 | 进入雨夜；留出旁白与脚步声空间 |
| 0:20–0:55 | 克制的疑虑 | 箫声引入，暗示山路深处有未明之物 |
| 0:55–1:25 | 微弱前行、仍不安 | 古琴与箫形成短句呼应，推动抵达客栈 |
| 1:25–2:00 | 收束、悬而未决 | 回到可无缝循环的稀薄织体，等待场景切换 |

**情绪关键词**：孤独、悬疑、冷雨、克制、未明。

**禁止方向**：欢快江湖曲、热血战斗节奏、浓烈情歌、跳脱电子音色、突发惊吓音效。

---

## 2. 音乐语言与配器

### 2.1 总体曲风

- 古风氛围配乐，低饱和、留白充足；以半写意的听感呼应项目“水墨半厚涂”视觉。
- 速度建议：**自由速度 / 约 56–64 BPM 的稀疏脉冲**；避免明确鼓点。
- 调式建议：以五声音阶或羽调式倾向组织；允许少量不完全终止制造悬念。
- 动态上限：整曲维持低至中低能量，不能压过开场旁白、湿靴脚步和推门等关键 SFX。

### 2.2 必用声部

| 声部 | 乐器与演法 | 功能 | 编写约束 |
|---|---|---|---|
| 主旋律 | 箫 | 以断续、带气息的短句承载“山路未尽”的方向感 | 不宜连续炫技；句末留空，避免完整明亮收束 |
| 和声/骨架 | 古琴 | 散音、泛音、低频单音与极简勾挑 | 音符稀疏；避免密集轮指与强烈节拍感 |
| 环境底层 | 连续雨声（独立环境轨） | 建立雨夜空间与遮蔽感 | 不作为音乐节奏器；使用自然、无雷暴、无强风的稳定雨幕 |
| 可选极弱层 | 低频空气垫 / 远处风声 | 补足空间，不可抢占主乐器 | 仅在 0:55–1:25 轻微抬升；不得出现现代合成器存在感 |

### 2.3 禁用素材

- 鼓组、锣钹重击、战斗打击乐。
- 现代 EDM、明显钢琴琶音、强节奏 Lo-fi 鼓点。
- 人声演唱、对白采样、可辨识的哭喊或尖叫。
- 雷声、爆裂风声；这些若后续剧情需要，必须独立作为 SFX 触发，不能固化进 BGM 循环。

---

## 3. 循环与文件规格

### 3.1 小样循环设计

- **目标循环总时长**：**2:00**（120 秒）小样；最终量产版可扩展至 PDD 规定的 **2:30**。
- **循环区间**：建议 `0:08.000 → 2:00.000`；前 8 秒保留作首入的雨幕与古琴散音引子。
- **循环方式**：循环首尾必须使用同一雨声床、相近空间残响尾音，并在箫/古琴均未形成完整终止时回接；禁止可感知的点击、突断、音高跳变或混响尾音被切断。
- **切场行为**：进入客栈场景时，不硬切 BGM；由 AudioManager 以 1.0–1.5 秒淡出，并交给 `bgm_inn_daily` 淡入。雨声应独立留在 Ambient/SFX 轨，依据场景继续或切换。

### 3.2 文件交付

| 项目 | 小样要求 | 说明 |
|---|---|---|
| 主文件 | `bgm_rain_intro_sample.ogg` | 48 kHz、立体声、OGG Vorbis；用于 Godot Web 与桌面验证 |
| 可选制作母带 | `bgm_rain_intro_sample.wav` | 48 kHz / 24-bit；仅供混音留档，不直接放入 Web 包 |
| 循环标记 | `rain-night-intro-loop-notes.txt` | 写明 loop_start、loop_end、淡入淡出与版本号 |
| 环境雨声 | `sfx_rain_continuous_sample.ogg` | 可独立循环，供 SFX/Ambient 轨验证；不烘焙到 BGM 主文件 |

**Godot 路径预留**：

```text
res://assets/audio/bgm/bgm_rain_intro_sample.ogg
res://assets/audio/ambient/sfx_rain_continuous_sample.ogg
```

---

## 4. 混音与音量层级

### 4.1 三轨职责

项目采用 Godot `BGM / SFX / Ambient` 三 AudioBus：

| AudioBus | 本小样素材 | 运行时职责 | 建议总线初始值 |
|---|---|---|---|
| `BGM` | 箫、古琴、极弱空间层 | 承担情绪与叙事推进 | `-12 dB` |
| `Ambient` | 连续雨声、可选远风 | 持续空间感，不抢对白 | `-22 dB` |
| `SFX` | 湿靴脚步、推门等后续触发声 | 与互动/文字节点同步 | `-10 dB` |

> 如果现有工程仅保留 `Master / BGM / SFX / Ambient`，雨声必须优先接入 `Ambient`；PDD 中 `sfx_rain_continuous` 的命名不改变其运行时环境音职责。

### 4.2 源素材相对层级

| 元素 | 相对响度目标 | 峰值约束 | 说明 |
|---|---|---|---|
| 箫 | 0 dB 基准 | 不高于 `-8 dBFS` | 最清晰，但不可压迫旁白 |
| 古琴 | 比箫低 3–5 dB | 不高于 `-11 dBFS` | 以空间和骨架为主 |
| 雨声 | 比箫低 8–12 dB | 不高于 `-18 dBFS` | 连续存在但不掩盖文字/SFX |
| 可选远风/空气层 | 比箫低 14–18 dB | 不高于 `-24 dBFS` | 只在听感过空时使用 |
| 关键 SFX（运行时） | 比 BGM 高 2–4 dB | 不高于 `-6 dBFS` | 由 SFX 总线与事件控制，不写死在 BGM 内 |

### 4.3 母线与可懂度控制

- 小样整合响度建议：**约 -20 LUFS（integrated）**；真峰值不高于 **-1 dBTP**。
- 混音不使用激进压缩或砖墙限制器；保留古琴与箫的瞬态和呼吸。
- 在 BGM 运行时，文本/旁白出现不必自动 duck；如实测中文旁白或关键 SFX 被遮挡，可在 AudioManager 添加**最多 3 dB、150–250 ms** 的临时 BGM 下潜。
- 对 Web 首屏保持轻量：OGG 小样目标不超过 **4 MB**；雨声单独压缩后目标不超过 **1.5 MB**。

---

## 5. Godot 淡入、循环与切换逻辑

### 5.1 首次进入雨夜开场

1. 场景加载时先启动 `Ambient` 雨声：`0.8–1.0 秒`淡入至 `-22 dB`。
2. 延迟 `0.3 秒`后启动 `BGM` 小样：`1.2 秒`线性或等功率淡入至 `-12 dB`。
3. 箫声首句应在 BGM 淡入接近完成后出现，避免首屏声音突发。
4. 浏览器/Web 导出受自动播放策略限制时，必须在玩家首次点击“开始/继续”后启动上述播放流程；不把失败当作资源缺失。

### 5.2 循环中

- 主 BGM 按 0:08–2:00 的指定区间无缝循环；环境雨声独立循环。
- 每轮循环不得叠加新的雨声实例；同一场景只保持一个 BGM 和一个雨声播放器。
- 暂停、存档读档后恢复时，优先恢复当前轨道与音量状态；不强制从头播放造成听感跳变。

### 5.3 切换至客栈

- BGM：`1.0–1.5 秒`淡出至静音；随后加载 `bgm_inn_daily`。
- Ambient：山道雨声可在角色进门节点后 `0.8 秒`淡至 -∞；客栈夜晚环境音按场景决定淡入。
- 若推门 SFX 在淡出期间触发，SFX 总线维持独立，不跟随 BGM 淡出。

---

## 6. 小样验证流程与通过标准

### 6.1 制作侧检查

- [ ] 箫、古琴、雨声均可单独导出/静音核验。
- [ ] 2:00 循环点首尾无点击、突断或明显节拍错位。
- [ ] 无强鼓点、现代电子音色、战斗感或突发恐怖音效。
- [ ] 箫句、古琴散音与雨声满足“孤独、悬疑、克制”的情绪目标。
- [ ] OGG 与可选 WAV 文件命名、采样率、声道数符合第 3 节要求。

### 6.2 Godot 混音验证

- [ ] BGM、Ambient、SFX 三总线可独立静音及调音。
- [ ] 雨声运行在 Ambient（或符合当前项目的独立环境音总线定义），不会随 BGM 切换重复叠加。
- [ ] 运行 `scene_001_rain_night` 时：雨声先入、BGM 后入，且无爆音。
- [ ] 以 `1.2 秒`淡入播放时，箫首句不突兀；以 `1.0–1.5 秒`淡出切换时无硬切。
- [ ] 同时播放湿靴脚步 SFX 时，脚步清晰，箫与古琴不被完全遮蔽。
- [ ] Web 导出在用户交互后可正常起播；首次加载不因音频资源使首屏超过 PRD 的 5 秒目标。

### 6.3 验收判定

- **通过**：制作侧与 Godot 混音验证全部通过；美术总监确认声音气质与“低饱和冷调水墨”视觉一致；音频设计师确认可作为后续 `bgm_inn_daily` 与角色主题曲的混音基准。
- **需调整**：循环可感知、雨声盖住文本、箫过亮/过满、古琴节奏感过强、Web 首屏体积超标任一项成立。
- **本任务当前范围**：本文件完成后，只代表“制作规范已就绪”。台账 P0-T4 必须在实际 `bgm_rain_intro_sample.ogg` 小样制作完成并通过上述混音验证后，才可标记 `completed`。

---

## 7. 评审结论（规范稿）

**美术总监 / 音频设计评审：通过作为小样制作基线。**

本规范严格绑定《清风渡》的雨夜开场、古琴/箫/雨声主体、低饱和水墨视觉与 Godot 三 AudioBus 技术架构。下一步由音频设计师按本规范制作 OGG 小样，并在 `gotbotgame` 工程完成循环、淡入、场景切换与 Web 起播验证。
