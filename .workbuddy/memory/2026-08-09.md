# 2026-08-09 工作日志

## P1 第四轮修改：文案分支显示 + UI 布局 + 数值状态栏（用户四需求，总监评审通过）
- 依据《outputs/清风渡-MVP文案总汇.md》修改，登记台账 P1-T9（completed，47 任务完成 17，进度 36.2%）。
- **需求1 文案分支显示**：`mvp_ui_controller.gd` 补全 32 个「选项→对应对白分支节点」映射（第二章三线 12 + 第三章柳喝茶5/黎线索4/归对坐4/闲逛3/伙计4），旧映射是 3 个已不存在的死代码选项 ID；新增「回应→继续→目标节点」流程——选完选项先展示分支内容（说话人标题+文案），只显示一个「继续」按钮，点击后才进目标节点，修复了分支文本覆盖目标文本导致第三章章节卡被跳过的问题；「继续」按钮仅绑定本次新增按钮，避免 queue_free 延迟删除误连。
- **数据层**：`liu_lushu_day1.json` 拆分「独自闲逛」——`scene_daily_explore` 改为仅入口句「你离开众人视线，在客栈四处走动。」，后院全文抽到新增节点 `scene_daily_explore_backyard`（此前进入闲逛即显示后院全文、选后院再重复显示）。
- **需求2 布局**：`main.tscn` ChoicePanel 上移加高 (72,82,620,368)=286px，按钮 40px/间距 8px（5 选项 232px 不再被对话框遮挡）；状态栏改双行 (36,16,760,78)，与选项面板 4px 间隔；对话框 (370-680) 与面板零重叠。
- **需求3 数值**：`mvp_scene_runner.gd` 新增 `_sync_day_slot_from_node()`（节点→天/时段同步，行动/返回节点继承当前时段）；修复 action_count 只写 flags 不写 state.action_count 属性、存档快照恒 0 的问题。`status_bar` 重构双行：Row1=时间+存档按钮、Row2=洞察/碎片/柳黎归好感；时间随节点推进，第一二章/章节卡/结局隐藏（用上午/下午/夜，不用晨/午/夜）；碎片与好感计数随 state_changed 实时刷新（如 li_clue_b=黎好感+15/碎片+1 累加正确）。
- **校验**：新增 tools/verify_round4.py（32 映射完整性+断链）、tools/simulate_flow.py（12 节点时间推进+时段最多5选项）、tools/check_gd_balance.py；JSON 解析、GDScript 括号平衡、全流程模拟全部通过。
- **评审**：产品设计/技术/美术三总监 + 制作人终审通过，见 `gotbotgame/P1_ROUND4_UI_TEXT_STATUS_REVIEW.md`。遗留：Godot 4.7.1 headless 挂起（P1-T6）未重试，需图形化编辑器人工验收（选项不遮挡、回应展示、时间推进、计数刷新）。

## 修复 render_choices 强类型数组报错（P1-T9 缺陷）
- 报错：`_show_choice_response: Invalid type in function 'render_choices' ... argument 1 (Array) does not have the same element type`（Godot 运行时强类型校验）。
- 根因：`choice_panel.gd` 的 `render_choices(choices: Array[Dictionary])` 为强类型数组参数；`_show_choice_response` 传入无类型数组字面量 `[{"choice_id":..., "text":...}]`（默认 `Array`），类型不匹配。另一调用点 `_render_current_scene` 传 `runner.get_choices()`（本就返回 `Array[Dictionary]`）无此问题。
- 修复（方案1，推荐）：调用侧构造强类型数组 `var continue_choices: Array[Dictionary] = [...]` 再传入；未改 choice_panel.gd 签名与业务逻辑、未改 JSON。备选方案2为把签名弱化为 `render_choices(choices: Array)`，未采用。
- 台账 P1-T9 notes 已追加修复记录；括号平衡校验通过。

## 启动 P2-T3 并规划子项进度
- 依赖校验通过（P2-T1/P2-T2 completed），台账 P2-T3 置 in_progress（started_date 2026-08-09）。
- 子项规划：S1 时段场景骨架模板（day_slot_template.scene.md）→ S2/S3/S4 第一二三天各3时段场景脚本（day*_slot*.scene.md）→ S5 节点映射校验+总监评审。
- 文案基线：时段卡与5行动入口文本严格取自 liu_lushu_day1.json（即《目前8.8.docx》已写内容），节点ID/选项/效果与 branch-tree-mvp.json 的 9 个 daily 节点一一对应（P2-T4 并行模式已确认）。

## 更正：P2-T3 内容早已完成（用户质疑核实）
- 用户质疑「P2-T3 是否已经开发好」。核实结论：**内容层面早已完成**——branch-tree-mvp.json 含 9 个时段节点（day1_01~day3_03，各 5 行动选项/效果/归汉双段/返回路由/日收束），liu_lushu_day1.json 含 9 张时段卡+6 行动入口文案，运行时已接入可玩（P1-T9 刚修过 UI 与路由）；P2-T4 notes 亦记录「T3 内嵌于 branch-tree daily 节点，用户确认并行完成」。唯一缺口是 scenes/ 下无独立 day*_slot*.scene.md 文档（台账交付物字段写的文件名，从未单独产出）。
- 台账修正：P2-T3 从 in_progress 恢复为 **completed（2026-08-08）**，deliverables 改为实际产出形式（branch-tree-mvp.json 内嵌 + liu_lushu_day1.json），notes 标注独立 scene md 为「文档完备性补充，非开发缺口」。summary 恢复 17/47 in_progress 0。
- 教训：规划前先核实台账备注与既有数据（分支树/对话JSON/运行时），避免把已内嵌完成的内容当未开发任务重新规划。

## P2-T6 伙计暗线+隐藏线 完成（总监评审通过）
- 依赖校验通过（P2-T3/T4 completed），台账 P2-T6 → completed（2026-08-09），summary 18/47（38.3%）。P2 剩 P2-T8（走水夜）、P2-T10（文案润色）未开始。
- 交付：`scenes/innkeeper_secret.scene.md`（掌柜秘密：虎口剑疤/古玉侵蚀/第三张桌续茶/账本/密道诱饵，三出口→警觉/账本标记/诱饵标记）、`scenes/innkeeper_third_explore.scene.md`（第三次闲逛判定：洞察≥4或识破虎口疤→解锁隐藏线；exposed&&!recognized_scar→死亡【淹死在自己的好奇心里】）、`scenes/tea_ending.scene.md`（得闲饮茶结局，文本取自已写文案零改写）、`tools/verify_p2t6.py`。
- **数据修复**：numerical-design.json 补 4 个 flag 定义缺口——observed_account_book / solo_night_explore / sacrifice_choice / survived（原被 ending_conditions 引用但未在 flags 表登记）。
- 评审：产品设计/技术总监+制作人终审通过，见 `P2_T6_INNKEEPER_REVIEW.md`。遗留：走水夜(P2-T8)后与隐藏线衔接待图形化编辑器联调。

## P2-T8 走水夜事件脚本 完成（总监评审通过）
- 依赖校验通过（P2-T3 completed），台账 P2-T8 → completed（2026-08-09），summary 19/47（40.4%）。P2 仅剩 P2-T10（全剧本文案润色）。
- 交付：`scenes/fire_night.scene.md` + `tools/verify_p2t8.py`。叙事严格基于 PDD 角色档案走水夜细节：柳第一个冲进去/扇骨燎黑了也没松手、黎搬水湿毛巾刀背接应、归汉剑尖点地落地、事后拆剑穗丝线塞柳门缝补扇；玩家 4 选项参与（上楼接应/楼下帮黎/fire_night_warned 灯油警觉差分/找伙计）；衔接隐藏线（走水夜后探第三张桌→tea_ending）与死亡结局【替人挡了刀】触发说明。
- 校验：PDD 细节 12 项关键词全覆盖（曾改写 PDD 原句「扇骨燎黑了也没松手」被校验拦截，已忠实补回）、字段完整、结局文案存在。
- 评审：产品设计/技术总监+制作人终审通过，见 `P2_T8_FIRE_NIGHT_REVIEW.md`。遗留：事件接入分支树与数值固化留待 P4 集成。

## Git 提交推送（Hailey0158/qingfengdu-game）
- 用户确认仓库为 `https://github.com/Hailey0158/qingfengdu-game`（非 github-china/github-pages，后者非本人仓库已澄清），要求提交并推送所有项目文件（dist 经用户确认排除，仅推项目文件）。
- 处理：.gitignore 补充 `.godot/`（Godot 编辑器缓存）、`*.tmp`、`~$*`（Office 临时文件）；清理已误暂存的 `gotbotgame/.godot/` 缓存、`outputs/~$渡-MVP文案总汇.md`（Office 锁文件，index+工作树删除）；amend 合并。
- 推送成功：`0f2a539..bc57c81 main -> main`，远端与本地 HEAD 一致（bc57c81）。本次含 2 提交：8a2fb4e（台账+skill）、bc57c81（MVP-P2 完整产出，251 文件，10835+ 行）。
- 安全提醒：用户提供的 GitHub token（ghp_ 开头 PAT）在对话中明文出现，已建议尽快在 GitHub 设置中轮换/撤销。

## Web 导出 pck 引用修复（build/web/index.html）
- 报错背景：网页尝试加载 index.pck，实际打包文件为 game.pck（Failed loading file 'index.pck'）。
- 修改：`gotbotgame/build/web/index.html` 第 114 行 `GODOT_CONFIG.fileSizes` 中 `"index.pck":82679780` → `"game.pck":82679780`（唯一 1 处，只做字符串替换，未动其他代码）。验证：index.pck 残留 0 处、game.pck 就位、220 行不变、体积 5447→5446。
- 用户决定：① 不推送（github-china/github-pages 为公共仓库无写权限，暂仅本地准备，先前已澄清真实仓库为 Hailey0158/qingfengdu-game）；② 不替换 dist/index.html（dist 与 build/web 是两次导出，pck 大小不同：82683748 vs 82679780；dist 若后续部署需同样替换）。

## 两端一致修复 index.pck → game.pck（用户改选彻底修复）
- 背景：build/web 于 14:59 被 Godot 重新导出覆盖，上次引用修改被冲掉；网页仍报 Failed loading file 'index.pck'（部署端实际文件为 game.pck）。
- 执行（用户确认「两端一致彻底修复」）：① `gotbotgame/build/web/index.pck` 与 `dist/index.pck` 重命名为 `game.pck`（82659384 / 82683748）；② 两个 index.html 的 fileSizes 引用 `"index.pck"` → `"game.pck"`。
- 验证：两目录 html 引用与磁盘 pck 文件名、体积完全一致；全项目 0 处 index.pck 残留；game.pck 共 2 个。
- 注意：下次 Godot 重新导出 Web 会再次生成 index.pck 并覆盖 index.html 引用，需再次执行本修复（或导出后统一改名）。

## git 仓库恢复 + build/web 入库 + 部署到 GitHub Pages 根路径
- **.git 丢失事件**：rebase 与并发 git 操作冲突导致 game-02/.git 目录消失。恢复：clone 远端（Hailey0158/qingfengdu-game，含网页上传提交 bcb52b4/824043c/d139b16）到 game-02-recover → 以本地工作区 tar 覆盖（排除 .git/.godot/dist/cache）→ 删除远端误上传的空文件 gotbotgame/build → commit d7451d5 → push 成功 → 把 .git 移回 game-02 并清理 recover。
- **build 入库**：gotbotgame/build/web 9 文件（game.pck 82MB/index.html 引用 game.pck 修复/index.wasm 等）提交入库；.gitignore 移除 build/ 排除。
- **根路径部署**：用户反馈 https://hailey0158.github.io/qingfengdu-game/ 404（仓库根无 index.html）。将 build/web 9 个运行文件复制到仓库根并 push（4c4ea10），根路径变为 HTTP 200、标题「清风渡 - MVP」、game.pck 引用正确。
- **GitHub 大文件警告**：game.pck 78.83MB 超过 GitHub 50MB 推荐值，建议后续用 Git LFS。
- **token 安全**：用户 token（ghp_ 开头）两次在对话明文出现，已多次提醒轮换。
- 线上技术检查：index.wasm MIME=application/wasm ✅、单线程模式（THREADS_ENABLED=false 无需 COOP/COEP）✅。

## 关键修正：Godot 引擎按 executable 推断 pck 名（game.pck 方案错误，恢复 index.pck）
- 用户浏览器控制台仍报 Failed loading file 'index.pck'（引擎请求 qingfengdu-game/index.pck 404）。
- **真正根因**：Godot 4 Web 引擎用 `GODOT_CONFIG.executable`（"index"）推断 pck 文件名（index.pck），**不看 fileSizes 键名**。此前把 pck 改名 game.pck 只改了 fileSizes 键名和文件，引擎仍请求 index.pck → 404。
- **正确修复**：pck 文件名恢复为 index.pck（根目录 + gotbotgame/build/web 两处 mv），index.html 的 fileSizes 引用改回 "index.pck"。提交 9fac493 + d001582 推送。
- 线上最终验证：index.pck 200 / index.wasm 200 / game.pck 404（冗余已清）/ 根 index.html 200。
- 教训：Godot 4 Web 部署，pck 必须与 executable 同名（index.pck）；改 fileSizes 键名无效。

## 设计：结算页+走水夜+结局路由衔接（P2_SETTLEMENT_ENDING_DESIGN.md）
- 用户要求：三天九时段结束后进结算页（统计洞察/碎片线索/三好感），走水夜在结算页之后，过渡只需几句话，能推出全部结局。
- 产出 `gotbotgame/P2_SETTLEMENT_ENDING_DESIGN.md`：流程 day3_close(去terminal)→scene_day3_summary(结算页：数值统计+关键flag差分文案)→scene_fire_night_transition(过渡几句话)→scene_fire_night(复用P2_T8四选项)→outcome(死亡分支)→scene_post_fire_night(得闲饮茶入口)→scene_route_split(合作/独行，独行含信错了人陷阱)→scene_ending_gate(16结局按数值优先级路由)。
- 关键设计：结局区间用顺序裁剪（cb只需>=30，cp>=70先行过滤）；MVP条件引擎已支持；20结局中4个(前方无路/淹死/信错了人/替人挡了刀)在前置流程，16个路由+2死亡入口均可达；结算页用对话框文本展示(不新建独立面板UI)。
- 待用户确认后实现：branch-tree-mvp.json 33→57节点 + final-dialogue 结局文案。

## P2-T11 结算页+走水夜+结局路由 实现完成（总监评审通过）
- 用户确认方案并新增要求「独立结算面板」。
- 交付：`scenes/settlement_panel.tscn`+`scripts/settlement_panel.gd`（卷轴风独立面板：洞察/线索碎片/柳黎归好感+5项flag关键发现差分+「熄灯睡下」按钮）；`main.tscn` 挂载面板；`branch-tree-mvp.json` 33→59节点（day3_close去terminal→summary→transition(过渡几句话)→fire_night(复用P2-T8四选项)→outcome(死亡分支)→post_fire_night(得闲饮茶入口)→route_split(合作/独行)→solo_gate(信错了人陷阱)→ending_gate(16结局路由)→18个ending节点，叙事取endings-full零改写）；`final-dialogue` 新增6段旁白；`mvp_ui_controller.gd` 背景映射26项+面板显示逻辑+_on_settlement_continue；生成/校验/模拟脚本3个。
- **附修2个缺陷**：① 既有lt条件bug——9个时段节点归汉双段用 {"op":"lt","value":1} 但 runner 只支持 gte/lte/eq/neq，lt 被静默忽略致双段选项同显，改为 lte 0（9处）；② 陌生人结局不可达——pass兜底(fragments>=4)无好感约束先于stranger拦截，pass限定 fragments<=7 后 stranger 捕获 f>=8 低好感场景。
- 校验：59节点无断链/全图可达/条件合规/路由模拟12场景全过/GDScript平衡通过。
- 台账 P2-T11 completed，summary 20/48（41.7%）。评审见 `P2_SETTLEMENT_ENDING_REVIEW.md`。遗留：图形化编辑器联调（结算面板显示/走水夜选项/结局展示）。

## P2-T10 全剧本文案润色 完成（总监评审通过）
- 依赖校验通过（P2-T1/T2/T3/T8/T9 completed），台账 P2-T10 → completed（2026-08-09）。**P2 阶段 11 项全部完成，P2 阶段 status → completed**；summary 23/48（47.9%），in_progress 0，blocked 1（P1-T6）。current_phase 仍 P1。
- 交付：`data/final-dialogue/mvp/liu_lushu_day1.json`（schema v3，73 节点）+ `P2_T10_POLISH_REVIEW.md`。
- 润色 7 类改动：① 文本与逻辑解耦——移除约 30 处正文内嵌裸数值（好感/洞察/碎片 ±N、识破提示），数值反馈由状态栏+结算面板承载；② 修语病（笑得/地/地）；③ 体例统一（第三日暂告段落→一段落、'谢'→"谢"、视角统一）；④ 修复重复 JSON key（scene_daily_gui_sit 两次定义，后者覆盖前者，改为 first 版本独立）；⑤ 移除选项回显前缀（A./C. 开头）；⑥ 补全 2 个缺失旁白节点（scene_post_fire_night 走水夜后「那壶茶，还是温的」、scene_ending_gate 结局判定前）；⑦ 角色语气防 OOC 收口（黎初识不写"放松"越阶情绪，PDD arc 初识=礼貌保持距离）。
- 零改写：branch-tree-mvp.json 选项/路由/数值、endings-full.json 与 18 个 ending 节点叙事全部未动。
- 校验：73 节点解析/mvp-inline 引用无缺失/31 响应映射完整/无残留数值/引号成对/无重复 key 全过。
- 教训：P2-T11 留了 scene_post_fire_night 与 scene_ending_gate 两个 mvp-inline 引用节点无文本（运行时显示空白），润色时补齐；新增旁白需与周边节点氛围呼应（第三张桌→隐藏线铺垫）。

## P2-T12 结局自动收敛 + docx 未表现情节补充（总监评审通过）
- 用户两项需求：①结局不是选出来的、由剧情分支发展；②对照《目前8.8.docx》整理并补充未表现情节。台账新增 P2-T12（completed，summary 24/49=49.0%）。
- **需求①结局自动收敛**：`scene_ending_gate` 加 `auto_route: true`；`mvp_scene_runner.gd` 新增 `auto_route()`（按 choices 声明顺序=数值设计优先级取第一个满足条件者）+ `is_auto_route()`；`mvp_ui_controller.gd` 检测 auto_route 节点→只显示旁白+「继续」按钮（不渲染结局选项文本），点击后自动收敛唯一结局。第三次闲逛风险判定 `scene_daily_explore_third` 同样 auto_route（生死判定不由玩家选）。
- **需求②13 项补充全落地**（文本全部取自 docx 零改写）：S1 A1折返(scene_002_path_return, returned_early) / S2 C1缩回(花生赔罪) / S3 C2蹭饭(快哉快哉, ach_meet_noise) / S4-S5 当夜事件(A3→exposed, B3C3→watched, 夜访叙事节点) / S6 柳茶首次3选+后续5选分叉(liu_tea_first_done, e→liu_refused) / S7 柴火堆灯油(fire_night_warned 走水警觉前置) / S8 第三次闲逛判定(死亡【淹死在自己的好奇心里】+逃脱+洞察≥4解锁隐藏线, explore_count 计数) / S9-S12 私约三线(柳观澜阁+15碎片1/黎托付地图+15碎片1 li_map_entrusted/归听故事+15碎片1 clue_old_grudge+item_sword_tassel；每晚 scene_private_check auto_route 按好感≥30 优先级触发，scene_private_close 按 action_count 回日收束，*_private_done 单角色不重复) / S13 对峙五路接入(route_split/solo_gate→scene_confrontation，A劝归汉gui+10洞察2/B让归汉决定gui+5洞察1/C救谢li+15 sacrifice_choice/D指出古玉truth_route_ready洞察3需洞察≥6碎片≥8/E旁观三好感-5；A-D→ending_gate 数值路由，E→ending_pass 清风过客)。
- 分支树 59→79 节点，final-dialogue 73→102 节点，numerical-design 新增 13 个 flag 登记。
- 校验全通过（verify_gap_supplement.py + verify_auto_route.py）：target 完整/mvp-inline 全覆盖/条件合规（仅 gte/lte/eq/neq）/全图可达/无乱码/flag 登记/GDScript 平衡/UI 背景映射全覆盖（新增 21 节点）。
- 教训：①新增响应文本节点只需在 final-dialogue 定义，**分支树中不要建同名节点**（explore_firewood 误建后不可达被删）；②静态 BFS 可达性需用"乐观可达"（条件选项视为可满足），否则条件路由节点误报不可达；③Python 脚本内嵌中文引号与字符串引号冲突需转义或用 \u201c。
- 遗留：私约触发时序/auto_route 按钮/对峙节点渲染待 P1-T8 图形化编辑器联调。
