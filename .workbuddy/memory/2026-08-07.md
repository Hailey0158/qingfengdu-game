# 2026-08-07 工作日志

## Godot 导入目录同步
- 用户指定 `C:\Users\gh604\WorkBuddy\game-02\gotbotgame` 为 Godot 导入目录；已将 P0-T2 原型工程的 `project.godot`、`default_bus_layout.tres`、scripts/scenes/data/assets/fonts 与 `TESTING.md` 同步进去。
- 保留原有 `icon.svg` 和 `.godot/` 缓存，并备份此前生成的项目配置为 `project.godot.before-prototype.bak`。
- 校验通过：标准目录与文件齐备、JSON 数据可解析、所有 `res://` 静态资源路径无断链、主场景与 5 个 AutoLoad 已配置；仍需在 Godot 4.x 执行实机测试后验收 P0-T2。

## P0-T3 美术风格验证
- 已完成柳陆书单人风格验证稿并经美术总监评审通过：`gotbotgame/assets/sprites/style-concept.png`。
- 规格为 1600×2000、RGBA 透明 PNG；确认青衫、墨竹折扇、黑发高髻、平静单表情，以及低饱和冷调半厚涂与水墨晕染为后续三名主角立绘统一基准。

## P0-T4 音频方向验证
- 已输出 `rain-night-intro-audio-spec.md`：为开场 BGM `bgm_rain_intro` 定义箫、古琴、独立雨声环境轨的曲风、2 分钟小样循环、三 AudioBus 音量层级、Godot 淡入/切场逻辑与 Web 验证标准。
- 已完成 `bgm_rain_intro_sample.ogg`、`sfx_rain_continuous_sample.ogg` 及对应 48kHz/24-bit WAV 母带；小样时长 120 秒，OGG 体积约 395KB 与 1.4MB，均通过编码可读性和规格核验。
- 已按箫+古琴 BGM、独立雨声 Ambient 的方案完成方向验证，并更新 P0-T4 为 completed；该程序化小样仅作混音方向验证，不等同最终作曲成品。

## P0-T2 Godot 4.7.1 实机验证
- 用户提供 Godot 4.7.1 路径后，使用 `D:\Godot_v4.7.1-stable_win64.exe\Godot_v4.7.1-stable_win64_console.exe` 成功导入并运行 `gotbotgame/` 原型。
- 修复 `default_bus_layout.tres` 缺少 `AudioBusLayout` 资源类型头造成的 Godot 4.7.1 解析错误；同步修复至 `godot-prototype/`。
- 新增仅用于 P0 实机验收的 `gotbotgame/scripts/p0_verification_runner.gd`；正常项目启动下 17/17 自动检查通过，覆盖 Control UI、中文打字机/跳过、JSON 分支与 `user://` 存读档、三 AudioBus、OGG 淡入、200ms 立绘 Tween 与黑屏转场。
- 新建 `gotbotgame/export_presets.cfg` 并成功 Web 导出 `gotbotgame/build/web/`；构建约 52MB。P0-T2 保持进行中，仍需 Chrome/微信浏览器人工检查首屏、首次交互音频、中文字体和跨刷新存档。

## Godot 4.7.1 批量解析错误修复
- 修复 `gotbotgame/default_bus_layout.tres` 的 `AudioBusLayout` 类型头，并同步修复 `godot-prototype/default_bus_layout.tres`，解决 AudioBus 资源缺少 `type` 的解析错误。
- 为 5 个 AutoLoad 脚本新增不与单例名冲突的 `Qingfengdu...` class_name；跨脚本引用改为 `/root/<AutoLoadName>` 的强类型获取，补齐 GDScript 变量与参数类型，解决单例 Identifier 未声明及推导错误。
- 一次性验证器移至 `gotbotgame/tests/disabled/p0_verification_runner.gd.disabled`，不再参与编辑器解析；未影响原型功能。
- Godot 4.7.1 重新导入与主场景 headless 启动均通过，无 Parse Error。完整文件、AutoLoad 配置和编辑器操作见 `gotbotgame/GODOT_4_7_1_FIX_GUIDE.md`。

## Web 中文字体方框修复
- 定位到网页端方框的根因：`init_font.gd` 原先未挂到 `scenes/main.tscn`，且只覆盖根节点 `default_font`，Web 端控件没有稳定使用项目内中文字体。
- 已将 `init_font.gd` 挂到主场景根节点；启动时加载 `CMSFont-Bold.TTF` 并递归为所有 Control 设置 `font`、`normal_font`、`bold_font` 覆盖。字体文件已检查覆盖当前原型中文文本。
- 已更新 `gotbotgame/fonts/README.md`，说明 Web 字体必须随资源打包、后续新增文案需做字形覆盖检查；Godot 主场景 headless 启动打印“中文字体加载成功”，Web 构建已重新导出到 `gotbotgame/build/web/`。

## P1-T3 柳陆书线分支树
- 用户要求继续完成未完成任务；依台账启动 P1-T3，依赖 P1-T2 已完成。
- 新增 `gotbotgame/data/branch-tree-mvp.json`，作为 MVP 柳陆书线运行时唯一分支源：7 个可达节点、13 个选择、2 个终止节点，覆盖雨夜开场、回头/前方无路彩蛋、豪客试探、客栈初识、柳陆书第一天与第一日收束。
- 接入 `mvp_scene_runner.gd` 读取 branch-tree-mvp.json、应用 on_enter_effects、按洞察值过滤古玉追问，并让终止节点隐藏选项；同步修正文档场景目标、角色视觉锚点与 PDD 选择命名。
- 产品设计总监初审通过，技术总监复核图结构无断链，制作人终审通过；Godot 4.7.1 重新导入、主场景 headless 启动和 Web 导出通过。未加入战斗/抽卡/卡牌/收集，也未越界实现 P2 全量内容。
- 台账已将 P1-T3 标记 completed；下一可启动任务为 P1-T4 基础 UI 实现（依赖 P1-T1 已完成）。

## P1 主场景与柳陆书线接入
- 用户要求完善 `main.tscn` MVP 骨架并接入柳陆书线；已完成 P1-T1/P1-T2 的产出与验收。
- `main.tscn` 接入 `mvp_ui_controller.gd` 和 `mvp_scene_runner.gd`，保留字体、音频、对话框、选项面板、状态条、立绘占位和转场组件；选项点击可驱动场景数据、状态变化和音频启动。
- 新增三段场景脚本：`scenes/001_rain.scene.md`、`scenes/010_meet.scene.md`、`scenes/020_liu_day1.scene.md`；新增 `scripts/mvp_scene_runner.gd` 与 `scripts/mvp_ui_controller.gd`，接入雨夜开场→初识→柳陆书第一天的 MVP 路由。
- SceneManager 状态同步改为显式 `/root/GameStateManager` 获取；Godot 4.7.1 重新导入、主场景 headless 启动和 Web 导出通过。
- 台账已将 P0 阶段标为 completed、P1-T1/P1-T2 标为 completed；P1 保持未完成，下一步是 P1-T3 柳陆书线分支树。

## P1-T1 Godot 工程搭建
- 用户确认 P0-T2 浏览器人工验收已闭环；已校验 P1-T1 依赖 P0-T2/P0-T5 均满足，将台账 current_phase 切换为 P1，并将 P1-T1 标记为 in_progress。
- 基于现有 gotbotgame 原型复用 5 个既有 AutoLoad、主场景、UI/字体/音频验证能力；新增 `scripts/scene_manager.gd` 与 SceneManager AutoLoad，注册 prototype、雨夜开场、初识、柳陆书第一天 scene_id 到路径，保留未产出场景的安全失败提示。
- `project.godot` 名称更新为“清风渡 - MVP”；GameStateManager、BranchEngine、SaveManager、UIController 注释切换到 P1 语义。Godot 4.7.1 重新导入、主场景 headless 启动和 Web 导出均通过，待完成台账验收。

## P1 MVP 流程规划
- 2026-08-07 根据台账规划 P1 MVP：P0-T2 浏览器人工验收仍是正式启动闸门；P1-T2 柳陆书线场景脚本可作为内容准备，P1-T1 工程搭建需等待 P0-T2 完成。
- 规划依赖链：P1-T1 → P1-T4/P1-T6；P1-T2 → P1-T3 → P1-T5；P1-T2 → P1-T7；P1-T1、P1-T3、P1-T4、P1-T5、P1-T6、P1-T7 全部完成后进入 P1-T8 集成测试，目标 M1 MVP 可玩。

## Web 音频无声修复
- 人工校验发现无声后，定位根因：P0 主场景没有绑定 `prototype_bgm`，没有调用播放方法；`audio-triggers.json` 为空属于正式触发规则尚未进入 P3/P4，不是音频文件静音。BGM 与雨声 WAV 均核验为 48kHz、立体声、120秒且有有效波形。
- 已在 `scripts/audio_manager.gd` 预加载 BGM/ Ambient OGG；首次点击任一验证选项时，在同一用户手势中启动 BGM 淡入和连续雨声，适配 Web 浏览器自动播放限制。
- 已将两条 OGG 导入参数改为 `loop=true`，主场景补齐 `ChoicePanel` 引用；修复旧测试入口残留变量和节点引用后，Godot 4.7.1 重新导入、主场景 headless 启动与 Web 导出均通过。
- 已更新 `assets/audio/README.md` 与 `GODOT_4_7_1_FIX_GUIDE.md`，人工复测需强制刷新后点击任一选项检查 BGM/雨声；P0-T2 仍待用户确认浏览器实际可听。

## P0/P1 工程差距扫描
- 2026-08-07 对 `gotbotgame` 做只读审计，按 P0 技术验证原型与 P1 MVP 正式工程需求分类；未修改代码、未删除文件、未更新任务状态。
- 发现 P0 残留集中在 `data/branch-tree.json` 原型测试树、`dialogue_box.gd` 技术验证文本、`status_bar.tscn` 的“验证存档/验证读档”、`save_load.tscn` 原型界面、`ui_controller.gd` 原型音频/测试入口、`audio_manager.gd` 原型混音入口、`tests/disabled/p0_verification_runner.gd.disabled` 归档验证器及相关 P0 文档/占位 JSON。
- 可复用模块包括 GameStateManager 基础状态容器、SaveManager JSON user:// 基础读写、SceneManager 路由骨架、MvpSceneRunner + `branch-tree-mvp.json` 的柳陆书 MVP 分支、DialogueSystem 打字机、AudioBus/BGM/Ambient/Web 首次交互解锁、字体递归覆盖与 Web 导出配置。
- 关键缺口：正式主菜单不存在；SceneManager 注册的 `001_rain.tscn`、`010_meet.tscn`、`020_liu_day1.tscn` 实际未产出；SaveManager 仍是单一 prototype 槽位且无自动存档/刷新恢复闭环；GameStateManager 未覆盖完整 MVP 存档字段；音频仍使用 P0 sample；立绘仍为 ColorRect 占位。当前仅形成 P1 骨架，尚未满足“开始→对话分支→保存→刷新读取”的 MVP 基线。

## P1-T4 基础 UI
- 2026-08-07 正式启动并完成 P1-T4：`dialogue_box.tscn`、`choice_panel.tscn`、`status_bar.tscn` 已去除 P0 技术验证文案与测试信号，改为正式旁白/角色名接口、卷轴水墨浅色对话框、左侧 ▷ 选项、朱砂悬停/按下反馈、清风渡天数/时段/洞察状态栏。
- `dialogue_box.gd` 新增 `show_text(speaker, text)`；`choice_panel.gd` 保持 MVP 运行器兼容并改用 `choice_id`；`status_bar.gd` 将时段显示为晨/午/夜并使用正式化提示文案；旧 `ui_controller.gd` 仅保留通用转场/立绘兼容能力。
- 产品设计总监与制作人评审通过；Godot 4.7.1 重新导入、主场景启动、Web 导出、分支 JSON 7/7 节点可达和 diff 检查通过。台账 P1-T4 已标记 completed，P1 完成 4/8。

## P1-T5 基础存档系统
- 2026-08-07 完成 P1-T5：扩展 `game_state_manager.gd` 为 PDD 对齐的完整快照，包含 scene/node/path、天气、天数/时段、行动次数、洞察、碎片、三角色好感、flags、线索、物品、已解锁结局和游戏时长。
- 重写 `save_manager.gd`：支持 `slot_01`~`slot_10`、`slot_auto`、`slot_day_end`；使用 `user://saves`、JSON schema `qingfengdu.save.mvp.1`、临时文件+绝对路径原子替换、槽位列表与摘要、版本校验、损坏存档失败信号。
- `mvp_scene_runner.gd` 进入节点后自动写入 `slot_auto`；状态栏手动保存/读取改为正式 `slot_01`。Godot 4.7.1 启动无解析错误，修复一次目录相对路径改名失败后，自动存档实际落盘并通过 JSON 结构检查，Web 导出通过。
- 技术总监复审与制作人终审通过；台账 P1-T5 标记 completed，P1 完成 5/8。主菜单启动恢复和页面刷新自动读取仍需后续集成。

## P1-T6/T7 占位美术与柳陆书线文案
- 已从用户提供的《目前.docx》提取并落地 P1 范围的雨夜开场、豪客试探、客栈初识、窗边柳陆书互动及第一日收束；新增 `gotbotgame/data/final-dialogue/mvp/liu_lushu_day1.json`，作为独立文案层覆盖 10 个 MVP 节点和三种柳陆书回应，逻辑继续由 `branch-tree-mvp.json` 驱动。
- 文案经产品设计总监与制作人审阅：保留短句、冷光、留白，柳陆书保持半文半白、以玩笑试探；未提前泄露二十年前真相、未越界加入三天九时段或 P2 结局内容。JSON 解析通过，P1-T7 已完成。
- 用户新增柳陆书立绘参考图，已归档为 `gotbotgame/assets/placeholder_sprites/liu_lushu_reference_placeholder.jpg`，与 PDD 黑发高髻、青灰长衫、墨竹折扇、含笑试探锚点一致；背景方案收敛为雨夜山道、客栈大堂、窗边饮茶三组青黑白水墨色调。
- P1-T6 资源接入与扫描排查：清理了指向不存在源文件的 `.import` 残留，新增三张 1280×720 PNG 背景占位（雨夜山道、客栈大堂、窗边饮茶），将 `character_sprite.tscn` 和 `main.tscn` 接入 TextureRect；柳陆书参考 JPG 安全加载，主场景引用/JSON/图片静态检查通过。Godot 4.7.1 无头扫描不再出现 signal 11，但仍在 `first_scan_filesystem` 16% 挂起，90 秒硬超时返回 124；Web 导出同样未产出，P1-T6 与 Web 验收继续 blocked。排查记录见 `gotbotgame/P1-T6_RESOURCE_SCAN_REPORT.md`。
- P1-T6 静态完整性审计：按用户硬约束只读检查 `main.tscn`、MVP 背景/立绘引用、6 个 JSON、脚本 `res://` 路径、11 个 `.import`、6 个 Autoload 与 7 个 `class_name`。主资源链和 JSON 均完整，未发现孤立 `.import`；发现 `scripts/scene_manager.gd` 登记的 `res://scenes/001_rain.tscn`、`010_meet.tscn`、`020_liu_day1.tscn` 不存在，列为中风险但未修改。报告为 `gotbotgame/P1-T6_STATIC_AUDIT_REPORT.md`，明确区分图形化编辑器可验证项与无头构建阻塞。

- 2026-08-07 美术组并行产出 P1-T6 占位资产：保留柳陆书用户参考 JPG，新增 1600×2000 RGBA 占位 `assets/placeholder_sprites/gui_han_placeholder.png` 与 `li_keying_placeholder.png`，新增/保留 3 张 1280×720 背景。`main.tscn` 新增 LiuPortrait/LiPortrait/GuiPortrait；`mvp_ui_controller.gd` 设定大堂显示三人、窗边仅显示柳陆书；旧 `ui_controller.gd` 的 PrototypePortrait 残留改为 LiuPortrait 路径。静态检查通过：JSON 可解析、15 条 res:// 引用无缺失、角色节点存在、无孤立 `.import`。交付评审见 `gotbotgame/P1-T6_ART_ASSET_REVIEW.md`：美术总监通过占位门槛，产品设计总监通过范围一致性，技术总监静态通过/GUI 待验，制作人条件通过；P1-T6 仍不改为 completed，已知 Godot 4.7.1 无头扫描阻塞保持不变。

- 2026-08-07 归汉、黎客颖拆分参考立绘：保留双人生成源图 `assets/generated_portraits/workbuddy-generated-HRlVaCuaypgoMDIrkpDNrAYon9Nq_ID0B-PKqkpuDSA.png`，以保守近白背景 Alpha 蒙版拆分并导出 `assets/sprites/gui_han_calm.png`、`assets/sprites/li_keying_calm.png`。两张均为 1600×2000 RGBA PNG，透明通道范围 0–255 且含居中角色可视边界；保持低饱和冷调、水墨半厚涂和平静单表情。当前为 Godot 可用的美术参考资源，尚未绑定 P1 运行时，P1-T6 阻塞状态不变。
- 2026-08-07 黎客颍单人立绘重做：首稿 `gotbotgame/assets/generated_portraits/workbuddy-generated-wk8LLlo6tb74ZZi-jIIw9twSkadluuJaBe5qxef29kc.png` 因体型过瘦被驳回，第二稿 `gotbotgame/assets/generated_portraits/workbuddy-generated-WTdhSeoLAfZVVClUNzk2ySXD5lef07wbT65atvmspIY.png` 又因体型过壮被驳回，第三稿 `gotbotgame/assets/generated_portraits/workbuddy-generated-pnjcY2UmghcUPWIZD01N0Sww23Nat-lmasL_XjPE2to.png` 的清瘦精悍体型获用户认可，但侧头与抱刀姿势需修正。现基于第三稿高保真重绘正脸背刀版 `gotbotgame/assets/generated_portraits/workbuddy-generated-k8zoUcefdVhX-zgOuj3Rd9HqMs_VtlKBGeNUtcZ3d-8.png`：脸与双眼正对观众、双手自然放松，朴素无纹长刀连鞘斜背于背后；保留暗红高马尾、褪色红绳、黑灰束袖劲装、清瘦精悍体型、低饱和冷调、工笔线稿、局部半厚涂和淡墨晕染。当前仅供用户/美术总监视觉审稿，尚未规范化为 1600×2000、未接入 Godot，P1-T6 状态不变。
- 2026-08-07 归汉黑发视觉试稿：用户最初选择“仅做黑发试稿”，生成 `gotbotgame/assets/generated_portraits/workbuddy-generated-eRNYZ3bk8s88MqYfPsW-EpOl7Gu9qSdjKYWxB_OpYIw.png`；后续用户将该图与黎客颍、柳陆书图一并确认为当前 MVP 立绘，因此归汉墨黑长发已提升为 MVP 正式视觉锚点并同步 PRD/PDD，白袍、长剑、淡蓝剑穗、冷淡内敛与无相宗身份不变。
- 2026-08-07 三角色 MVP 立绘集成：三张来源图均为 1122×1402 RGB、棋盘格烘焙背景；新增 `tools/prepare_mvp_portraits.py` 以边缘连通分离背景并等比裁放到 1600×2000 RGBA，输出 `assets/sprites/mvp/gui_han_calm_mvp.png`、`li_keying_calm_mvp.png`、`liu_lushu_calm_mvp.png`。`main.tscn` 与 `character_sprite.tscn` 已引用新资源，1280×720 下三人约64%屏高并统一脚底基线，大堂左/中/右为归汉/黎客颍/柳陆书，窗边仅柳陆书。静态检查通过：Alpha有效、res路径无缺失、节点存在、6个JSON可解析、无孤立import。评审见 `gotbotgame/P1-T6_MVP_PORTRAIT_INTEGRATION_REVIEW.md`；本轮未执行 Godot GUI/无头构建，P1-T6 仍 blocked。
- 2026-08-07 三角色真实透明底复核：因原运行时 PNG 被进程占用，未强制覆盖或关闭编辑器，安全生成 `assets/sprites/mvp/gui_han_calm_transparent.png`、`li_keying_calm_transparent.png`、`liu_lushu_calm_transparent.png` 并切换 `main.tscn` / `character_sprite.tscn` 引用。三张均为 1600×2000、PNG 色彩类型6（RGBA）、Alpha 0–255；四角和整圈画布边缘 Alpha=0，全透明像素比例分别为72.14%、73.72%、66.70%。场景路径、三角色节点、6个JSON及孤立import静态检查全部通过；保持大堂三人、窗边仅柳陆书。未执行既知会挂起的无头构建，P1-T6 仍 blocked，待图形化编辑器人工验收。
- 2026-08-07 Web 端中文乱码根因与修复：审计确认所有 `.gd`/`.tscn`/`.json` 均为合法 UTF-8，乱码并非编码异常，而是旧字体 `CMSFont-Bold.TTF` 的 cmap 不映射任何中文字符（项目全部 585 个非 ASCII 字符均为 MISSING）。下载开源 Noto Sans CJK SC（OFL）放入 `fonts/NotoSansCJKsc-Regular.otf`（16.4 MB），同步 `fonts/OFL-Noto-CJK.txt` 许可；`init_font.gd` 切换字体路径并新增 `get_tree().node_added` 信号，确保运行时动态创建的选项按钮也绑定中文字体；`choice_panel.gd` 显式加载中文字体，并将易缺字的三角引导符 `▷` 替换为稳定 ASCII `>`。字体字形验证：项目全部 585 个非 ASCII 字符 ALL_CHARACTERS_COVERED，包括 `·`、`→`、`—`、`…`、`“`、`”`、`▷` 全部覆盖。旧字体文件保留不动。
- 2026-08-07 客栈三人剧情大图：严格依据《目前.docx》与 `branch-tree-mvp.json` 的 `scene_010_meet` 设定——"大堂不算大，五六张桌，几盏油灯；窗边青衫男子歪在椅子里转墨竹折扇；右边桌红发黑衣年轻人低头看泛黄纸；最里侧白衣女子端坐如常，淡蓝剑穗随风轻晃"——生成 1536×1024 客栈大堂场景图 `assets/generated_portraits/A_wide_angle_cinematic_establi_2026-08-07T16-27-40.png`：左/窗边为柳陆书（青衫、黑发高髻、墨竹折扇含笑），中/后方为归汉（白袍、端坐、剑置身前），右为黎客颍（红发高马尾、黑衣、低头看纸、朴素长刀置桌旁）。三角色各自独立空间、未互动，契合"初识·试探·好奇"氛围。低饱和冷青灰、墨色晕染、半厚涂线稿方向。图片右下角带有 WorkBuddy AI 生成水印（系统自动附加），非人工添加。
