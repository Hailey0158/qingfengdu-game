# P0 技术原型资产目录

本目录只保留标准路径，禁止在 P0-T2 写入正式剧情、美术或成品音频资产。

- `sprites/`：P3 前仅允许技术占位。
- `backgrounds/`：P3 前仅允许技术占位。
- `ui/`：P0 仅允许布局与交互占位。
- `audio/bgm/`、`audio/sfx/`、`audio/ambient/`：P0 仅允许短测试音频；正式音频遵循 PDD 音频规格，在 P3 量产。
